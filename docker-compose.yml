version: "3.9"

services:

  api:
    build: .
    command: python -m uvicorn api.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"

  dashboard:
    build: .
    command: python -m streamlit run dashboard/streamlit_app.py
    ports:
      - "8501:8501"
    depends_on:
      - api